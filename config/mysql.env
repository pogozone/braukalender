# Braukalender - MySQL Konfiguration

# Environment Variablen für .env Datei
REACT_APP_STORAGE_TYPE=mysql
REACT_APP_MYSQL_HOST=localhost
REACT_APP_MYSQL_PORT=3306
REACT_APP_MYSQL_USER=root
REACT_APP_MYSQL_PASSWORD=your_mysql_password
REACT_APP_MYSQL_DATABASE=braukalender

# Server-Konfiguration (falls MySQL als Backend verwendet wird)
MYSQL_PORT=3306
MYSQL_HOST=localhost
MYSQL_DATABASE=braukalender

# Frontend-Konfiguration
REACT_APP_API_URL=http://localhost:3001/api/data

# MySQL Connection Pool (optional)
# REACT_APP_MYSQL_CONNECTION_LIMIT=10
# REACT_APP_MYSQL_ACQUIRE_TIMEOUT=60000
# REACT_APP_MYSQL_TIMEOUT=60000

# Hinweise:
# - MySQL muss installiert und laufen: sudo systemctl start mysql
# - Setup-Script ausführen: ./bin/setup-mysql.sh
# - Daten werden in relationalen Tabellen gespeichert
# - Gut für strukturierte Daten und komplexe Beziehungen
# - Unterstützt Transaktionen und ACID-Eigenschaften

# Installation MySQL (Ubuntu):
# sudo apt update
# sudo apt install -y mysql-server mysql-client
# sudo mysql_secure_installation
# sudo systemctl start mysql
# sudo systemctl enable mysql

# Datenbank-Benutzer erstellen (optional):
# mysql -u root -p
# CREATE USER 'braukalender'@'localhost' IDENTIFIED BY 'secure_password';
# GRANT ALL PRIVILEGES ON braukalender.* TO 'braukalender'@'localhost';
# FLUSH PRIVILEGES;
