# Braukalender - File Storage Konfiguration

# Environment Variablen für .env Datei
REACT_APP_STORAGE_TYPE=file

# Keine zusätzlichen Konfigurationen erforderlich
# Die Daten werden in der Datei 'termine.json' im Projektverzeichnis gespeichert

# Server-Konfiguration (falls eigenständiger Server verwendet wird)
API_PORT=3001
API_HOST=localhost

# Frontend-Konfiguration
REACT_APP_API_URL=http://localhost:3001/api/data

# Hinweise:
# - Dies ist die einfachste Konfiguration
# - Keine zusätzliche Software erforderlich
# - Daten werden lokal im JSON-Format gespeichert
# - Gut für Entwicklung und kleine Installationen
