# Braukalender - MongoDB Konfiguration

# Environment Variablen für .env Datei
REACT_APP_STORAGE_TYPE=mongodb
REACT_APP_MONGODB_URL=mongodb://localhost:27017/braukalender

# MongoDB Verbindungsoptionen (optional)
# REACT_APP_MONGODB_OPTIONS={"useNewUrlParser":true,"useUnifiedTopology":true}

# Server-Konfiguration (falls MongoDB als Backend verwendet wird)
MONGO_PORT=27017
MONGO_HOST=localhost
MONGO_DATABASE=braukalender

# Frontend-Konfiguration
REACT_APP_API_URL=http://localhost:3001/api/data

# Authentifizierung (optional)
# REACT_APP_MONGODB_USER=braukalender_user
# REACT_APP_MONGODB_PASSWORD=your_password

# Hinweise:
# - MongoDB muss installiert und laufen: sudo systemctl start mongod
# - Setup-Script ausführen: ./bin/setup-mongodb.sh
# - Daten werden persistent in MongoDB gespeichert
# - Gut für Produktionsumgebungen und große Datenmengen
# - Unterstützt komplexe Abfragen und Indizes

# Installation MongoDB (Ubuntu):
# sudo apt update
# sudo apt install -y mongodb
# sudo systemctl start mongod
# sudo systemctl enable mongod
